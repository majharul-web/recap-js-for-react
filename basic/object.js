const person = {
  name: 'jony',
  address: 'dinajpur',
  hoby: ['game', 'song', 'garden'],
  job: 'Engineer',
  salary: 70000,
};

// access property with .
person.salary = 45000;
console.log(person);

// access property with brackets
console.log(person['name'], person.hoby[0]);

console.log(person;
